# Plymouth Theme

Use this directory for the kernel/initramfs splash. A production theme should
provide a `.plymouth` descriptor, a script or animation module, and reviewed
logo/background assets.

The `.example` files document the expected interface without being installed
automatically. Keep the splash independent of the API so boot remains
diagnosable when Vestara services are unhealthy.
