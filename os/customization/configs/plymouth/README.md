# Plymouth Configuration

The selected profile determines whether the image installs the normal Vestara
splash or the recovery splash. Both must remain usable without the API.

Expected installed path:

```text
/usr/share/plymouth/themes/vestara/
```
