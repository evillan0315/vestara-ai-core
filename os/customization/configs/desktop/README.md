# Desktop Configuration

Desktop profile configuration covers display-manager selection, wallpaper,
font, icon, cursor, and motion preferences. The current minimal image builder
does not install a desktop environment; this configuration is the contract for
that future image variant.
