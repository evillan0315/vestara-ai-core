# @vestara/tool-runtime

Common registration, input validation, policy evaluation, cancellation,
invocation, and evidence boundary for Agent Harness tools.

The initial filesystem adapters preserve `FilesystemRuntime` as the owner of
workspace sandboxing, deny lists, filesystem observations, and low-level
operation history.
