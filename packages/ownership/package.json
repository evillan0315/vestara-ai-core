{
  "name": "@vestara/ownership",
  "description": "Resource ownership and locking — write access control with timeout and deadlock prevention (ADR-027)",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@vestara/types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
