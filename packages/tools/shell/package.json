{
  "name": "@vestara/tools-shell",
  "description": "Governed shell execution tool — bounded subprocess execution with approval-gated risk (ToolRuntime adapter)",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc"
  },
  "dependencies": {
    "@vestara/shared": "workspace:*",
    "@vestara/action": "workspace:*",
    "@vestara/tool-runtime": "workspace:*",
    "@vestara/types": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
