{
  "name": "@vestara/configuration",
  "description": "Configuration loader and file-based config source",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo 'TEST CORPUS RESET: currently zero tests — new suite pending'; exit 0"
  },
  "dependencies": {
    "@vestara/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
