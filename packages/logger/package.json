{
  "name": "@vestara/logger",
  "description": "Structured JSON logger with levels and transports",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo 'TEST CORPUS RESET: currently zero tests — new suite pending'; exit 0"
  },
  "dependencies": {
    "@vestara/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
