{
  "name": "@vestara/policy-types",
  "description": "Pure contracts for the Policy subsystem — PolicyDefinition, conditions, actions, scope, context, decision, and engine interfaces",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
