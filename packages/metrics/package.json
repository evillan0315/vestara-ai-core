{
  "name": "@vestara/metrics",
  "description": "Counters, gauges histograms for runtime observability",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "dependencies": {
    "@vestara/shared": "workspace:*",
    "@vestara/logger": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
