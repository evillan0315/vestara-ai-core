{
  "name": "@vestara/sqlite-migrations",
  "description": "Versioned, transactional, idempotent SQLite migrations for sql.js databases",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo 'TEST CORPUS RESET: currently zero tests — new suite pending'; exit 0"
  },
  "devDependencies": {
    "sql.js": "^1.14.1",
    "vitest": "^4.1.10"
  }
}
