{
  "name": "@vestara/health",
  "description": "Aggregate health checks for runtime components",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "echo 'TEST CORPUS RESET: currently zero tests — new suite pending'; exit 0"
  },
  "dependencies": {
    "@vestara/shared": "workspace:*",
    "@vestara/logger": "workspace:*",
    "@vestara/service-registry": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
