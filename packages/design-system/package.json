{
  "name": "@vestara/design-system",
  "description": "Renderer-neutral semantic design tokens and presentation metadata shared by the Workspace UI and terminal TUI",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --root ../.. packages/design-system/__tests__"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
