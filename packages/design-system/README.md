# @vestara/design-system

Renderer-neutral semantic design tokens for Vestara. Shared by the Workspace UI (React/web) and the terminal TUI (OpenTUI/React).

## Usage

```
pnpm --filter @vestara/design-system build
```

## Dependencies

None (leaf package).
