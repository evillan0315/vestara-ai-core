{
  "name": "@vestara/ci-reviewer",
  "description": "CI-OBS-001D stateless Reviewer kernel — pure CIFailureEvidence/CIFailureEvidence → CIClassification + promotion verdicts per 001D-001. No IO, no clock, no persistence.",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --root ../.. packages/ci-reviewer/__tests__"
  },
  "dependencies": {
    "@vestara/ci-contracts": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
