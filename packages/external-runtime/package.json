{
  "name": "@vestara/external-runtime",
  "description": "External coding-agent runtime observability — generic adapter protocol, registry, redaction, correlation, and runtime intelligence capture",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --root ../.. packages/external-runtime/__tests__"
  },
  "dependencies": {},
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
