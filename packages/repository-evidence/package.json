{
  "name": "@vestara/repository-evidence",
  "description": "Repository verification evidence adapter — failure fingerprints, environment fingerprints, and repository snapshot comparison (ADR-012 reference implementation)",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run --root ../.. packages/repository-evidence/__tests__"
  },
  "dependencies": {
    "@vestara/verification-evidence": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
