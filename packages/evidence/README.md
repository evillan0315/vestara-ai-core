# @vestara/evidence

Engineering evidence pipeline — collectors, content-addressed evidence, immutable manifests, verification bundles, replay descriptors, and derived confidence (PCS-026).

## Usage

Import via workspace reference:

```
pnpm --filter @vestara/evidence build
```

See [docs/](../../docs/) for capability specifications and architecture.
