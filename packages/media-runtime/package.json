{
  "name": "@vestara/media-runtime",
  "description": "Provider-neutral media conferencing contracts and runtime",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "echo 'TEST CORPUS RESET: currently zero tests — new suite pending'; exit 0",
    "build": "tsc"
  },
  "dependencies": {
    "@vestara/shared": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.10"
  }
}
