# @vestara/extension-contracts

Stable, provider-neutral contracts for installable Vestara packages. The
package owns manifest validation, lifecycle states, permissions, trust,
isolation declarations, contribution descriptors, and extension health.

These contracts contain no installer or activation policy.
