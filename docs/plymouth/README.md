# Plymouth Customization

Plymouth owns the kernel-to-systemd transition. Themes must remain readable,
quiet, and independent of API availability. The profile contract and expected
installation path are documented in `os/customization/configs/plymouth/`.
