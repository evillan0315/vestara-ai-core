# Login Customization

Login profiles are defined under `os/customization/profiles/` and select
terminal, recovery, or graphical presentation. The current image builder
defaults to a headless/developer-safe environment until a display manager and
desktop stack are explicitly added.
